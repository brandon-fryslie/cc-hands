"""The domain, pure: state, events, effects, and the reducer that relates them.

Imports the standard library's data modules and nothing that reaches the world;
tests/test_core_boundary.py holds it to that.
"""
